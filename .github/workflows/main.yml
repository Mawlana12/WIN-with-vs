name: CI

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Download Visual Studio
        run: Invoke-WebRequest https://download.visualstudio.microsoft.com/download/pr/014120d7-3b3e-4399-a673-c7d60e3e918b/0c64d38d1b421b71453c21d13e5c24b5/vs_Community.exe -OutFile vs_community.exe
      - name: Install Visual Studio
        run: .\vs_community.exe --installPath "C:\Program Files (x86)\Microsoft Visual Studio\2019\Community" --quiet --wait --norestart
        shell: cmd
      - name: Download ngrok
        run: Invoke-WebRequest https://bin.equinox.io/c/4VmDzA7iaHb/ngrok-stable-windows-amd64.zip -OutFile ngrok.zip
      - name: Extract ngrok
        run: Expand-Archive ngrok.zip
      - name: Authenticate ngrok
        run: .\ngrok\ngrok.exe authtoken $Env:NGROK_AUTH_TOKEN
        env:
          NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
      - name: Enable Remote Desktop
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server'-name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 1
          Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "P@ssw0rd!" -Force)
      - name: Create ngrok TCP tunnel
        run: .\ngrok\ngrok.exe tcp 3389
